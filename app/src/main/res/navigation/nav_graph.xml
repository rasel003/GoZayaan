<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.rasel.androidbaseapp.ui.home.RecommendationFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_recommendation">
        <action
            android:id="@+id/action_nav_home_to_nav_gallery"
            app:destination="@id/nav_plantDetailFragment" />
    </fragment>

    <fragment
        android:id="@+id/nav_slideshow"
        android:name="com.rasel.androidbaseapp.ui.slideshow.SlideshowFragment"
        android:label="@string/menu_slideshow"
        tools:layout="@layout/fragment_slideshow" />

    <fragment
        android:id="@+id/nav_plantDetailFragment"
        android:name="com.rasel.androidbaseapp.ui.plant_details.PropertyDetailsFragment"
        android:label="PlantDetailFragment"
        tools:layout="@layout/fragment_plant_detail">

        <deepLink
            app:action="ACTION_VIEW"
            app:uri="www.example.com/{plantId}" />
        <argument
            android:name="model"
            app:argType="com.rasel.androidbaseapp.data.models.RecommendationModel" />
    </fragment>

   </navigation>